export default function tagColor (tag) {
    if (tag === "Hobbies/Passions") {
        return "#FF033E";
     } else if (tag === "Social") {
         return "#00BFFF";
     } else if (tag === "Health & Fitness") {
         return "#018749";
     }else return "#CD7F32";
}