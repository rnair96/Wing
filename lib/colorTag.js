export default function tagColor (tag) {
    if (tag === "Let's Build Something") {
        return "#FF033E";
     } else if (tag === "Let's Have Fun") {
         return "#00BFFF";
     } else if (tag === "Let's Train") {
         return "#018749";
     }else return "#CD7F32";
}