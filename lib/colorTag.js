export default function tagColor (tag) {
    if (tag === "Passions & Pursuits") {
        return "#FF033E";
     } else if (tag === "Social Outings & Adventures") {
         return "#00BFFF";
     } else if (tag === "Sports & Fitness") {
         return "#018749";
     }else return "#CD7F32";
}