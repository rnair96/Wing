export default function tagColor (tag) {
    if (tag === "Open to Build Something Cool") {
        return "#00BFFF";
     } else if (tag === "Open to Outdoor Activities") {
         return "#fd5c63";
     } else if (tag === "Open to Workout") {
         return "#32de84";
     } else if (tag === "Open to Charitable Services") {
        return "#CF9FFF";
    }else return "#CD7F32";
}